<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyDailyMoney" parent="Theme.Material3.Light.NoActionBar">
        <!-- Force Light Mode colors regardless of system setting -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.MyDailyMoney" parent="Base.Theme.MyDailyMoney" />
</resources>